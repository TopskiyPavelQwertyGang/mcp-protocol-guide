[project]
name = "mcp-protocol-guide"
version = "0.1.0"
description = "Minimal educational MCP server demonstrating tools, resources and prompts"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.12.0,<2",
]

[tool.uv]
package = false
